# 🚚 DeliveryCalc — Калькулятор оптимальной доставки

**DeliveryCalc** — сервис для расчёта стоимости доставки стройматериалов с разных производств.  
Он автоматически подбирает **оптимальный по цене маршрут**, учитывает **вес**, **тарифы**, **расстояния**,  
и позволяет добавлять **спецтранспорт (манипулятор, длинномер и т.д.)**.

---

## ⚙️ Основные возможности

- 🔹 Расчёт **по стоимости**, а не просто по расстоянию  
- 🔹 Учитывает **цены материалов** на каждом производстве  
- 🔹 Автоматический выбор **оптимального производства**  
- 🔹 Возможность добавить **манипулятор или спецтранспорт**  
- 🔹 Гибкая система тарифов (`tariffs.json`)  
- 🔹 Интерактивный интерфейс для менеджера  

---

## 🧮 Пример логики расчёта

> Пример:  
> Завод A — цена товара 150₽, доставка 120₽  
> Завод B — цена товара 170₽, доставка 90₽  
>  
> ➜ DeliveryCalc выберет **завод B**, потому что итоговая стоимость меньше (260₽ против 270₽)

---

## 📂 Структура проекта
delivery_calc/
├── backend/
│ ├── main.py # Основная логика API и расчёта
│ ├── factories.json # Список заводов и их координаты
│ ├── tariffs.json # Тарифы и характеристики транспорта
│ └── requirements.txt # Зависимости (FastAPI, etc.)
│
├── frontend/
│ ├── src/pages/Calculator.jsx # Основной интерфейс калькулятора
│ └── ... # Остальные файлы React
│
├── README.md
└── .env / Dockerfile (если используются)

---

## 🚀 Запуск локально

### 0. Настроить окружение (секреты не коммитить)
- Скопируйте `.env.example` в `.env` и подставьте свой `GOOGLE_SHEET_ID` и путь к ключу сервисного аккаунта.
- Скопируйте `google_credentials.example.json` в `google_credentials.json` и заполните реальными данными сервисного аккаунта.
- Эти файлы уже в `.gitignore`, поэтому реальные ключи не попадут в репозиторий.
- Если ключи или токены случайно попали в историю git, **считайте их скомпрометированными**: удалите их из истории, сгенерируйте новые ключи в Google Cloud и заново заполните `.env` и `google_credentials.json` из шаблонов.

### 1. Установить зависимости (Python 3.10+)
```bash
pip install -r requirements.txt

### 2. Запустить backend (Python 3.10+)
uvicorn main:app --reload

Backend будет доступен по адресу:
➡️ http://127.0.0.1:8000

### 3. Запустить frontend
cd frontend
npm install
npm run dev

Интерфейс откроется на:
➡️ http://localhost:5173

🧠 API endpoints
GET	/api/factories	Список всех производств
GET	/api/tariffs	Список тарифов транспорта
POST	/api/quote	Расчёт оптимальной доставки

### 4. Пример запроса:

{
  "upload_lat": 55.7558,
  "upload_lon": 37.6173,
  "transport_type": "auto",
  "addManipulator": true,
  "selectedSpecial": "Манипулятор SPECIAL",
  "items": [
    {"category": "Дорожные ПЛИТЫ", "subtype": "2п30.18.30 Гравий", "quantity": 10}
  ]
}

### 4. Пример ответа
{
  "итого": 167000,
  "общий_вес": 44,
  "количество_рейсов": 2,
  "транспорт": "🚛 Длинномер MAN TSG × 1, 🦾 Манипулятор NEXT (принудительно) × 1",
  "детали": [
    {
      "завод": "тубетон",
      "товар": "Дорожные ПЛИТЫ (2п30.18.30)",
      "машина": "Длинномер MAN TSG",
      "расстояние_км": 79.29,
      "стоимость_материала": 162000,
      "стоимость_доставки": 48000,
      "итого": 210000
    }
  ]
}

DeliveryCalc создан с целью упростить расчёт логистики
и сделать выбор маршрута максимально прозрачным для менеджеров.

Автор проекта: (Asmolu)
🟢 GitHub: Asmolu
